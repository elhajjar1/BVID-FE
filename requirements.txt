# Runtime dependencies for the bvidfe library, CLI, and Streamlit app.
#
# Tuned for Streamlit Community Cloud + local CLI/tests. Dev tools
# (pytest, ruff, black) live in the optional-extras of pyproject.toml.
#
# Streamlit Cloud installs these on top of Python 3.11. The ranges below
# pick the latest stable series that supports Python 3.10-3.12.

# --- Core (numerical + plotting) ---
numpy>=2.1,<3
scipy>=1.14,<2
matplotlib>=3.9,<4
shapely>=2.0,<3
pandas>=2.0,<3

# --- Web app ---
streamlit>=1.32
plotly>=5.18
